---
title: 博客搭建日志
date: 2018-09-30 16:34:20
tags: 
  - 博客日志

---
![hexo+git](/medias/common/hexo2.jpg)

## 工具：
博客代码&主题：[Hexo](https://hexo.io/zh-cn/) & [hexo-theme-matery](https://github.com/blinkfox/hexo-theme-matery)

代码托管平台：[GitHub](https://github.com/jiangwen5945/jiangwen5945.github.io)

源码编辑器： VScode


### VScode编写博客所需插件

> 安装`Markdown Preview Enhanced` 实现Markdown语法编写与实时预览

> 安装`paste image to qiniu`实现图片的本地备份与网络图床的上传(采用七牛对象存储)


***

## 过程记录要点

因为在本地`hexo g -d`操作上传到git上的只是生成的`public`静态博客页面文件夹，所以为了实现在多台电脑管理操作，我们在GitHub上建立分支dev-blog保存hexo源码.

具体操作如下：

### 博客源文件备份

```
$ git init      //初始化一个git仓库
$ git remote add origin git@github.com:icessun/icessun.github.io.git     //与远程仓库建立连接
$ git add .    //在本地把源代码提交到本地版本库中
$ git commit -m 'add hexo-blog origin code'  //添加到暂存区，并备注
$ git push origin master:dev-blog  //推送到远程仓库的源代码分支上面
```

这样就把源代码备份完毕，这个时候，可以在远程仓库里面看见dev-blog分支，里面保存着博客源代码


### 本地操作博客

要是源文件在本地，在自己的电脑上面，当在本地写好了博客的时候，可以先备份一下博客源文件，然后在部署静态文件

```
//提交到git分支
$ git add .
$ git commit -m 'add new blog'
$ git push origin master:dev-blog

//部署静态文件
$ hexo g -d

```


### 别人电脑pull源代码

首先要在别人的电脑上面安装好git、node、hexo，然后使用克隆clone的命令下载源代码：

> git clone -b dev-blog git@github.com:jiangwen5945/jiangwen5945.github.io.git // 克隆分支源码

```
npm install hexo    // 安装hexo  安装完成hexo 不需要初始化hexo，否者hexo 配置参数会重置
npm install    // 安装依赖库
npm  install hexo-deployer-git  --save    // 安装部署相关配置
```
接着同步文件源代码到github

```
$ git add .
$ git commit -m 'add new blog again'
$ git pull origin dev-blog //先拉取仓库的源代码到本地进行合并，解决不同版本的冲突
$ git push origin dev-blog  //再将现有的源代码文件push到dev-blog分支
```
最后发布博文,部署静态文件


> $ hexo g -d
